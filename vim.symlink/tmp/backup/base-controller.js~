/* global dependencies */
var _ = require('underscore');
var $ = require('jquery');
var Marionette = require('backbone.marionette');

module.exports = Marionette.Object.extend({
    /* Platform overrides */
    defaultLevel: null, // String, a key from objectViews
    objectViews: null,  // Object, map level strings to view classes

    browseCampaign: function(campaign, region) {
        throw new Error('Not implemented');
    }
});

module.exports = BaseController;
