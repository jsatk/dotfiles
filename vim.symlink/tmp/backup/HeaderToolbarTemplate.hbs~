{{!- CONTEXTUAL TOOLBAR --}}
{{#if showNavToolbar}}

<div id="toolbar" class="sc-navbar navbar-fixed">
    <div class="container">
        {{! Mobile menu }}
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#{{viewName}}-toolbar-options">
                Menu
                <i class="fa fa-fw fa-bars"></i>
            </button>
        </div>

        <div class="navbar-collapse collapse" id="{{viewName}}-toolbar-options">
            {{! Subpage navigation }}

            {{#each subNavs}}
            {{#if platform}}
            <ul class="nav navbar-nav">
                <div class="create-ad-nav">
                    <img src="{{bLogo}}" alt="brand_logo" class="toolbar-img">
                    <span>{{bName}} |</span>
                    <span class="create-ad-name">{{#if iURI}}<a href={{iURI}}>{{/if}}{{iName}}{{#if iURI}}</a>{{/if}} | </span>
                    <i class="fa fa-platform-{{platform}}"></i>
                    <span class="create-ad-name toolbar-second-name">{{cName}}</span>
                </div>
            </ul>
            {{else}}
            {{#if bLogo}}
            <ul class="nav navbar-nav">
                <div class="edit-nav">
                    <img src="{{bLogo}}" alt="brand_logo" class="toolbar-img" />
                    <span>{{bName}} |</span>
                    <span class="toolbar-second-name">{{iName}}</span>
                </div>
            </ul>
            {{else}}
            <ul class="nav navbar-nav menu">
                <li class="{{class}}"><a{{#if href}} href="{{href}}"{{/if}}>{{name}}</a></li>
            </ul>
            {{/if}}
            {{/if}}
            {{/each}}

            {{!-- {{#restrict 'STAFF'}} --}}
            <form role="form" class="form">
                {{#ifCurrentView viewName 'initiative'}}
                {{#canCreateInitiatives}}
                {{! New Initiative button }}
                <div class="form-group pull-right">
                    <button type="button" class="btn btn-highlight" id="create-initiative">
                        <span class="hidden-lth-sm">
                            <i class="fa fa-plus space-right"></i> New Initiative
                        </span>
                        <span class="hidden-gth-md">
                            <i class="fa fa-plus"></i>
                        </span>
                    </button>
                </div>
                {{/canCreateInitiatives}}
                {{/ifCurrentView}}

                {{#ifCurrentView viewName 'creative'}}
                {{! New Creatives button}}
                <div class="form-group pull-right">
                    <button type="button" class="btn btn-default" id="create-creative">
                        <i class="fa fa-plus"></i> New Creative
                    </button>
                </div>
                {{/ifCurrentView}}

                {{#ifCurrentView viewName 'creative'}}
                <div class="btn-group pull-right" data-toggle="buttons">
                    <label class="btn btn-default active">
                        <input type="radio" name="viewmode" autocomplete="off" value="table" checked>
                        <i class="fa fa-table"></i>
                    </label>
                    <label class="btn btn-default">
                        <input type="radio" name="viewmode" autocomplete="off" value="list">
                        <i class="fa fa-list-ul"></i>
                    </label>
                </div>
                {{/ifCurrentView}}

                {{#ifCurrentView viewName 'initiative-detail'}}
                {{! New Campaign button }}
                <div class="form-group pull-right">
                    <button type="button" class="btn btn-highlight" id="create-campaign" data-initiative="{{initiativeId}}" {{#if canNotCreateCampaign}} disabled {{/if}}>
                        <span class="hidden-lth-sm">
                            <i class="fa fa-plus space-right"></i> New Campaign
                        </span>
                        <span class="hidden-gth-md">
                            <i class="fa fa-plus"></i>
                        </span>
                    </button>
                </div>
                {{/ifCurrentView}}

                {{#ifCurrentView viewName 'campaign-details'}}
                {{#canManageInitiative brand_id initiative_id}}
                    {{#if_eq effectivePlatform 'facebook'}}
                        <div class="btn-group pull-right">
                            <a class="btn btn dropdown-toggle btn-highlight" data-toggle="dropdown" href="#"><i class="fa fa-plus"></i> New <span class="caret"></span></a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li class="{{#unless canCreateAd}}disabled{{/unless}}"><a href="#" id="create-ad">Ads</a></li>
                                <li><a href="#" id="create-post">Page posts</a></li>
                            </ul>
                        </div>
                    {{else}}
                        <form role="form" class="form">
                            <div class="form-group pull-right">
                                <button type="button" class="btn btn-highlight {{#unless canCreateAd}} disabled {{/unless}}" id="create-ad">
                                    <i class="fa fa-plus"></i> New ads
                                </button>
                            </div>
                        </form>
                    {{/if_eq}}
                {{/canManageInitiative}}
                {{/ifCurrentView}}

                {{#ifCurrentView viewName 'budget'}}
                <div class="btn-group pull-right" id="{{viewName}}-bulk">
                    <button type="button" class="btn dropdown dropdown-toggle" data-toggle="dropdown">
                        <span class="control-label hidden-lth-md">Bulk actions</span>
                        <i class="fa fa-caret-down hidden-lth-md"></i>
                        <i class="fa fa-fw fa-tasks hidden-gth-lg"></i>
                    </button>
                    <ul class="dropdown-menu flat-menu" role="menu">
                        <li><a href="#" data-action="budget">Set budget and bids</a></li>
                    </ul>
                </div>
                {{/ifCurrentView}}

                {{! Actions }}
                {{#hasFilterOptions actions}}
                <div class="btn-group pull-right" id="action-menu">
                    <button type="button" class="btn dropdown dropdown-toggle" data-toggle="dropdown" disabled="true">
                        <span class="control-value hidden-lth-sm">Actions</span>
                        <i class="fa fa-caret-down"></i>
                    </button>
                    <ul class="dropdown-menu flat-menu" role="menu">
                        {{#each actions}}
                        <li><a href="#" data-action="{{type}}">{{label}}</a></li>
                        {{/each}}
                    </ul>
                </div>
                {{/hasFilterOptions}}

                {{! Filter }}
                {{#hasFilterOptions filterOptions}}
                <div class="btn-group pull-right" id="{{viewName}}-filter">
                    <button type="button" class="btn dropdown dropdown-toggle" data-toggle="dropdown">
                        <span class="control-label hidden-lth-md">Filter by:</span>
                        <span class="control-value hidden-lth-md">All</span>
                        <i class="fa fa-caret-down hidden-lth-md"></i>
                        <i class="fa fa-fw fa-filter hidden-gth-lg"></i>
                    </button>
                    <ul class="dropdown-menu flat-menu" role="menu">
                        {{#each filterOptions}}
                        <li><a href="#" data-filter-type="{{type}}">{{label}}</a></li>
                        {{/each}}
                    </ul>
                </div>
                {{/hasFilterOptions}}

                {{! Date range }}
                {{#if dateFilterView}}
                <div class="btn-group pull-right" id="datepicker">
                </div>
                {{/if}}

                {{! Search }}
                {{#if showSearch}}
                    <div class="form-group has-feedback pull-right search-nav">
                        <input type="search" autocomplete="off" id="{{viewName}}-search" class="form-control" placeholder="Search">
                        <span class="fa fa-search form-control-feedback"></span>
                    </div>
                {{/if}}
            </form>
            {{!-- {{/restrict}} --}}
        </div>
    </div>
</div>
{{/if}}
