#! /usr/bin/env node

// Module dependencies
var cli = require('../lib/cli')

// Optionally passed in filename
var filename = process.argv.slice(2)[0]
console.log(cli.processInput)
// Run the command-line client
cli.parseInput(filename, cli.processInput)
