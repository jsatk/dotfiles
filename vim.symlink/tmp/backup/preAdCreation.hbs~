<form class="form" role="form">

    <!-- Stuff to fill out -->
    <section>
        <fieldset class="form-group">
            <input type="text" class="form-control" id="name" name="name" placeholder="Ad cluster name" value="{{name}}" required>
        </fieldset>

        <fieldset class="row">
            <div class="form-group col-md-6">
                <label for="start_date">Start date</label>

                <div class="input-group date start-picker-sc">
                    <input required type="text" class="form-control" id="start_date" name="start_date" value="{{startDate}}"
                        data-provide="datepicker"
                        data-date-end-date="{{endDate}}"
                        data-date-start-date="{{campaignStartDate}}"
                        data-today-highlight="true">
                    <span class="input-group-addon"><i class="fa fa-calendar"></i></span>
                </div>
            </div>

            <div class="form-group col-md-6">
                <label for="end_date">End date</label>

                <div class="input-group date end-picker-sc">
                    <input required type="text" class="form-control" id="end_date" name="end_date" value="{{endDate}}"
                        data-provide="datepicker"
                        data-date-end-date="{{campaignEndDate}}"
                        data-date-start-date="{{startDate}}"
                        data-today-highlight="true">
                    <span class="input-group-addon"><i class="fa fa-calendar"></i></span>
                </div>
            </div>
        </fieldset>
    </section>

    <!-- Button(s) -->
    <section>
        <div class="alert alert-danger hidden" role="alert" id="error"></div>
        <button type="submit" class="btn btn-primary {{#unless name}} disabled {{/unless}}" id="submit">Create ads</button>
    </section>

</form>
