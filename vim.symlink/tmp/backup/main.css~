body {
    background: gray;
}

.right-hand-rail {
    background: rgba(250, 250, 251, 0.980392);
    bottom: 0;
    height: 100%;
    position: fixed;
    right: 0;
    width: 370px;
}

.right-hand-rail .header,
.right-hand-rail .footer {
    background: white;
    position: absolute;
    width: 100%;
}

.right-hand-rail .header {
    border-top: 1px solid #dadee2;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.08);
    top: 0; /* This is super unnecessary right now. But I'd keep it here so that as you add shit or modify this this for sure stays up top. */
}

.right-hand-rail .stupid-fucking-wrapper-div-im-lazy {
    padding: 1em;
    overflow: hidden; /* This is an old trick/hack to deal with making containing block elements properly acknowledge and wrap floats. Why does it work? Because computers are the worst, that's why. */
}

.right-hand-rail button {
    margin: 0 0.5em;
}

.right-hand-rail button:first-child {
    float: left;
}

.right-hand-rail button:nth-child(2),
.right-hand-rail button:last-child {
    float: right;
}

.right-hand-rail .footer {
    border-top: 1px solid #dadee2;
    bottom: 0;
    box-shadow: 0 -1px 2px 0 rgba(0, 0, 0, 0.04);
}

.right-hand-rail input {
    background: rgba(250, 250, 251, 0.8);
    border: 1px solid #e4e4e4;
    padding: 0.5em;
    width: 100%;
}
