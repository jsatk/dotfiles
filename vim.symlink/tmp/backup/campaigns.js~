/* global dependencies */
var Backbone = require('backbone');

/* local dependencies */
var Model = require('../models/campaign');

module.exports = Backbone.Collection.extend({
    model: Model,
    url: Model.prototype.urlRoot,
    initialize: function() {
        debugger;
        this.filters = this.options.query;
    }
});
