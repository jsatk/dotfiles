<div class="sc-card-header">
    <div class="sc-card-photo">
        <img src="{{logo}}" alt="Profile picture">
    </div>

    <div class="sc-card-details">
        <div class="col-md-12">
            <a href="#initiative/{{parentInitiativeId}}">&laquo; {{titleize parentInitiativeName}}</a><br>
            <h2>{{name}}</h2>
        </div>

        <div class="col-md-3">
            <div class="sc-card-label">
                Today's spend
            </div>

            <div class="sc-card-value">
                {{formatMoney spend_today}} {{!-- <small>65 likes</small> --}}
            </div>
        </div>

        <div class="col-md-3">
            <div class="sc-card-label">
                {{#if dateIsInFuture}}
                Ending in
                {{else}}
                Ended
                {{/if}}
            </div>

            <div class="sc-card-value">
                {{timeToEndDate}}
                {{#unless dateIsInFuture}} ago {{/unless}}
                <small>{{endDate}}</small>
            </div>
        </div>

        <div class="col-md-3">
            <div class="sc-card-label">
                Total spend
            </div>

            <div class="sc-card-value">
                {{formatMoney spend_lifetime}}
            </div>
        </div>

        <div class="col-md-3">
            <div class="sc-card-label">
                Pacing
            </div>

            {{!-- NOTE: The pacing is here as a backup. This is replaced with the pacing meter. --}}
            <div class="sc-card-value pacing">
                {{add_commas pacing}}
            </div>
        </div>
    </div>
</div>

<div class="panel-body chart"></div>
