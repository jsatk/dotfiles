<h1> Hi </h1>
<div id="insertion-table-region" class="panel-heading">
    <h3 class="panel-title">Insertion Orders</h3>
</div>

<div class="panel-body">
    <div class="form-group">
        <select class="form-control" id="campaign-insertion-orders" name="campaign-insertion-orders" multiple="true">
            <option data-uri="none"></option>
            {{#each items}}
                <option data-uri="{{resource_uri}}" {{#if selected}} selected {{/if}}>
                    {{name}}{{#unless name}}{{url}}{{/unless}}
                </option>
            {{/each}}
        </select>
    </div>
</div>
